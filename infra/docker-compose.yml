# Entry point referenced by the README:
#   docker compose -f infra/docker-compose.yml up --build
# Delegates to the real base stack located in this folder (docker-compose/).
# Use the prod / test overrides from the same folder when needed:
#   docker compose -f infra/docker-compose/docker-compose.yml \
#                  -f infra/docker-compose/docker-compose.prod.yml up --build

include:
  - docker-compose/docker-compose.yml
