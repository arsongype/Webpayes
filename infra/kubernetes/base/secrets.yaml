# NOTE: These are placeholder secrets. In production generate real values and
# prefer a sealed-secrets / external-secrets / CSI driver approach.
#   echo -n 'my-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: payment-platform-secrets
  namespace: payment-platform
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME_production_db_password"
  JWT_SECRET: "REPLACE_ME_production_jwt_secret_min_32_chars_long"
